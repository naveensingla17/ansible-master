Ansible - CMT (Configuration Management Tool)

CMT:
  Ansible (*) - (yaml)
  Chef - (Ruby / erlang)
  Puppet - (Puppet DSL)
  saltstack - (yaml)

Master server (control node) --> remote server (Managed node)

Playbook = Task

Inventory = Host List


